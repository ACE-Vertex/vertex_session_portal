# LIVE DISPATCH ORIGIN + FLICKER RAY 000059V5H1

H1 repairs only the diagnostic probe's Windows console encoding failure.

It performs a bounded, CP932-safe READ-ONLY audit of current Session Portal production source and inspects:
- immutable capture origin,
- routing envelope mutation before SHA,
- staging metadata,
- Workstation registration payload/error mapping,
- VraDispatchLane polling/reconcile/render behavior,
- stable-vs-volatile card identity,
- DOM rebuild primitives around status/error transitions.

No production source file is modified.
The returned Evidence is the required anchor set for the narrow production repair VRA.
