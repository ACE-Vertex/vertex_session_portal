# Correlation + Incident Runtime Smoke 000079V4C2

Use after the manual `npm.cmd run build` completed successfully and after a
full Session Portal restart.

If MARK INCIDENT appears, click it once before running this smoke.

This pass additionally proves that the rebuilt `out/main/index.js` contains
both the correlation observer and incident marker code before checking runtime
events. Verification is read-only.
